# .env do gateway. Cada API filha lê o próprio .env em ../<pasta>/.env.
# Este arquivo aqui só configura o gateway e o compose orquestrador.

APP_ENV=dev            # dev | prod
GATEWAY_PORT=8081      # porta externa do host que expõe o gateway
